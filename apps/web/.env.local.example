# Copy to .env.local for `npm run dev` outside Docker. Inside docker-compose,
# this is set instead via the root .env (see NEXT_PUBLIC_API_BASE_URL there).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
