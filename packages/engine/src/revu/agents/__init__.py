"""Reviewer agents. `diff_only` (Stage 3) is the initial simplified reviewer; the cross-file
agent and tool layer (Stage 7) supersede it as the default once the indexer and context
retrieval stages exist.
"""
